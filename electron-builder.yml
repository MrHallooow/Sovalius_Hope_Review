appId: com.sovalius.hope-review
productName: "HOPE Review"
copyright: "Copyright (c) 2026 Sovalius Corporation"

directories:
  output: dist
  buildResources: build

# NOTE: no credential file is packaged anymore. The only runtime config is the
# gateway URL: bundled default in electron/config.production.js, overridable by
# the extraResources .env (GATEWAY_URL=... and nothing else).
files:
  - "electron/**/*"
  - "dist-renderer/**/*"
  - "node_modules/**/*"
  - "package.json"

forceCodeSigning: false

win:
  target:
    - target: nsis
      arch:
        - x64
  artifactName: "HOPE-Review-Setup-${version}.${ext}"
  # App icon: the Zeta mark, taken from the sovalius-mobile app icon
  # (ios/.../Icon-App-1024x1024@1x.png) so desktop and mobile match.
  icon: build/icon.ico
  # signAndEditExecutable:false skipped icon + version metadata as WELL as
  # signing, which is why builds shipped with the default Electron icon.
  # signExecutable:false skips ONLY the signing step.
  signExecutable: false

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  deleteAppDataOnUninstall: false
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: "HOPE Review"

extraResources:
  - from: ".env"
    to: ".env"

publish:
  provider: github
  owner: MrHallooow
  repo: Sovalius_Hope_Review
  releaseType: release
